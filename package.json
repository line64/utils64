{
  "name": "utils64",
  "version": "0.0.3",
  "description": "Set of methods and useful tasks for Line64",
  "main": "./lib",
  "scripts": {
    "lint": "eslint src",
    "test": "npm run compile && node test/test.js",
    "precompile": "rm -rf lib",
    "compile": "broccoli build lib",
    "prepublish": "babel src --out-file lib/index.js",
    "line64publish": "npm publish --registry=http://npm.line64.com"
  },
  "author": "Line64",
  "license": "ISC",
  "dependencies": {
    "lodash": "^4.5.0",
    "moment": "^2.11.2",
    "qs": "^6.1.0"
  },
  "devDependencies": {
    "broccoli": "^0.16.9",
    "broccoli-babel-transpiler": "^5.5.0",
    "broccoli-cli": "^1.0.0",
    "broccoli-concat": "^2.1.0"
  }
}
