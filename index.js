"use strict";

import { concatenateVariables } from "./src/concatenateVariables";
import { dateDifference, dateISOFormat, dateDisplayFormat } from "./src/dateUtils";
import { getQuery } from "./src/getQuery";
import { pluralize } from "./src/pluralize";

export default {
    concatenateVariables,
    dateDifference,
    dateISOFormat,
    dateDisplayFormat,
    getQuery,
    pluralize
};
